import React from 'react'
import styles from '../style'

const ContactButton = () => (
  <div>
    <div className={`${styles.ghostButton} text-[16px] py-4 px-6 w-52 flex justify-center`}>
    CONTACT US
    </div>
  </div>
)


export default ContactButton
